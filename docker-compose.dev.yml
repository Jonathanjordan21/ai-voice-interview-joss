version: "3.9"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.dev
      
    container_name: interview-frontend

    ports:
      - "5173:5173"

    volumes:
      - .:/app
      - /app/node_modules

    restart: unless-stopped